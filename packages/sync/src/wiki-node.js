// Placeholder — vollständige Implementierung im PoC (poc/yjs-libp2p)
// Dieses Package wird der produktionsreife Sync-Layer

/**
 * Erstellt einen Wiki-Sync-Node.
 * Kapselt libp2p + Yjs + GossipSub.
 */
export async function createWikiNode(_options = {}) {
  throw new Error('Not yet implemented — see poc/yjs-libp2p for the prototype')
}
